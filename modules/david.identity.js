define(function(){
    
    david.identity = {
        
        /**
         * If initialisation is part of an element, lets see if the element has content, or if we need to load it
         */
        init : function(toElement)
        {
            // Lets find the tab control:
        }
        
    };
    
    
    return david.identity;
    
});